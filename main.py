import argparse

parser = argparse.ArgumentParser(
    prog='PyCLIvity',
    description="CLI to-do list manager.",
    epilog='Now get to work!',
    )

def create_task()

def 

# Commands to have:
# main.py add -name --desc
#       returns Name (ID=) has been created!
# main.py update -id -status (0, 1 and 2)
# main.py delete -id
# main.py list --status (0, 1 and 2)
#       returns a display of the full dict of the task
